# This will allow pip installation in eqditable mode
# " pip install -e '.' "
# In this way, is not needed to be reinstalled, if changes
# if changes are made

from setuptools import setup

if __name__ == '__main__':
    setup()